#include<iostream>
#include<math.h>

using namespace std;

int main (){
    int a = 2, c=1, aux=0,aux2=0;
    float b = c-1+a, aux3=0;
    
    // aux = b * b > 4 ||  pow(a,4) > c * c + 20;
    // aux = 1 + 15 / pow(2,3) / b;
    //aux2 = b * (a+1) - b/a/10;
    // aux2 = c * (5%3); */;

    cout<<aux2<<endl;
    cout<<aux3;

}