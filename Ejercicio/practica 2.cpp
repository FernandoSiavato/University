#include<iostream>
#include<math.h>

using namespace std;

main(){

int a=3, b=4, r=0,x=3, y=7;
float q=0;
bool c = true;

// r = (a*2 > b) && c;
// r = b/2 - 4*a*b/1/2;
//  r = pow(b/2-4*a*b,1/2);
/* r = (b/float(2-4*a)*b);
q = pow((b/(2-4*a)*b),1/2); */
q = r = y % 2 + x * 2 - 2 / 2;

cout<<"\n soy q "<<q<<endl;

}